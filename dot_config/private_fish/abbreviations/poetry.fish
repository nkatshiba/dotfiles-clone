# Poetry
abbr -a po 'poetry-fzf'
abbr -a por 'poetry run python'
abbr -a porm 'poetry run python main.py'
abbr -a pora 'poetry run python a.py'
abbr -a porb 'poetry run python b.py'
abbr -a porc 'poetry run python c.py'

# add
abbr -a poa 'poetry add'

# pyright
abbr -a poetryinit '/home/tildavimer/src/scripts/app-specific/poetry/poetry_venv_config.fish'

# loop
abbr -a porl 'for file in (ls);kitten @ launch --type background --cwd=current poetry run python $file;end;'

# poetry run python alternatives
abbr -a p 'poetry-fzf'
