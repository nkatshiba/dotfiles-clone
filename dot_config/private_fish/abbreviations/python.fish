# python
abbr -a pyv 'python --version'
abbr -a venv 'source ../../.venv/bin/activate.fish && commandline -i "python "'
abbr -a venvp 'source ../../.venv/bin/activate.fish && commandline -i "pip install "'
