function fish_greeting

    # set COLOR (set_color 746589)
    # set NORMAL (set_color normal)
    #
    # set -l date_in_seconds (date -d "$CHEZMOI_BAK_DATE" "+%s")
    # set -l now_in_seconds (date "+%s")
    # set -l difference_in_seconds (math $now_in_seconds - $date_in_seconds)
    # set -l difference_in_days (math -s0 $difference_in_seconds / 86400)
    #
    # if test $difference_in_days -ge 3
    #     echo $COLOR"> $difference_in_days day(s) since bak ($CHEZMOI_BAK_DATE)"
    # else
    #     set -l date 12-dec-2023
    #     set -l future_date (date -d "$date + 3 days" "+%d-%b-%Y")
    #     echo $COLOR"> should pop at $future_date"
    # end

end
